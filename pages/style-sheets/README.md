# Website Style Awards — reference build v2

This version is rebuilt around the supplied reference screenshot:
- editorial cream/black visual system
- exact-style header + utility row
- large serif "STYLE SHEETS" hero
- SVG geometry diagram and labeled nodes
- five interactive style-system cards
- featured dark editorial panel with target graphic
- principles/resource sidebars
- clickable reference links with icons
- search overlay, theme toggle, responsive layout
- reduced-motion support

Open `index.html` directly in a browser.
Internet is only needed for the Google Fonts import. The core visuals are SVG/CSS and work locally.
